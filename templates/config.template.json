{
  "_comment": "Skill-install template. Owner only fills composio.apiKey + composio.defaultUserId. Everything else (which platforms, which toolkits, which userIds) is discovered at setup time by querying Composio for connected accounts under defaultUserId. Restaurant info comes from the owner via Telegram onboarding → restaurant-profile.json. {{AGENT_HOME}} is replaced by install.sh with the agent's data dir.",
  "composio": {
    "apiKey": "",
    "defaultUserId": "",
    "userIdOverrides": {},
    "mcpServerUrls": {},
    "mcpServerIds": {}
  },
  "platforms": {
    "_comment": "Auto-discovered by setup.js based on which toolkits are connected under defaultUserId in Composio. Hand-edit only if you want to force-disable a connected platform.",
    "instagram": { "enabled": false },
    "tiktok":    { "enabled": false },
    "facebook":  { "enabled": false }
  },
  "googleDrive": {
    "_comment": "enabled is auto-set by setup.js if googledrive is connected in Composio. folderName defaults to the restaurant name once onboarding completes.",
    "enabled": false,
    "folderName": "",
    "localCachePath": "{{AGENT_HOME}}/social-marketing/photos/",
    "inventoryPath": "{{AGENT_HOME}}/social-marketing/photo-inventory.json"
  },
  "analytics": {
    "bookingTracking": { "method": "manual", "dailyBaseline": 0 },
    "utmSource": "social"
  },
  "imageGen": {
    "_comment": "Two backends: 'azure' is a direct OpenAI-compatible endpoint (Azure or your own proxy), 'composio' goes through Composio MCP. primary controls which is tried first; fallbackOnError swaps to the other on failure. Default model is gpt-image-2. azure.apiKeyEnv names the env var that holds the key (do NOT paste the key here).",
    "primary": "composio",
    "fallbackOnError": true,
    "model": "gpt-image-2",
    "azure": {
      "baseUrl": "",
      "apiKeyEnv": "AZURE_OPENAI_API_KEY",
      "deployment": ""
    },
    "composio": {
      "toolSlug": ""
    }
  },
  "research": {
    "_comment": "Override the model used by weekly-research.js. Default: perplexity/sonar (web search built in). Routed through OPENROUTER_CHAT_COMPLETIONS via Composio MCP.",
    "model": "perplexity/sonar"
  },
  "timezone": "Europe/Stockholm",
  "country": "SE",
  "posting": {
    "_comment": "schedule: array of HH:MM (24h, local timezone) when auto-post fires. Add multiple for lunch+dinner posts. autoPost.enabled toggles the whole feature; .paused is set when the owner says 'pause posting' / 'resume posting' on Telegram; .lastPostedAt and .consecutiveFailures are written by auto-post.js — leave blank initially.",
    "schedule": ["11:00"],
    "autoPost": {
      "enabled": true,
      "paused": false,
      "lastPostedAt": null,
      "consecutiveFailures": 0,
      "pausedUntil": null
    }
  },
  "paths": {
    "baseDir": "{{AGENT_HOME}}/social-marketing/",
    "restaurantProfile": "{{AGENT_HOME}}/social-marketing/restaurant-profile.json",
    "knowledgeBaseDir": "{{AGENT_HOME}}/social-marketing/knowledge-base/",
    "posts": "{{AGENT_HOME}}/social-marketing/posts/",
    "reports": "{{AGENT_HOME}}/social-marketing/reports/",
    "trendReports": "{{AGENT_HOME}}/social-marketing/reports/trend-reports/",
    "competitorReports": "{{AGENT_HOME}}/social-marketing/reports/competitor/",
    "errors": "{{AGENT_HOME}}/social-marketing/errors.json",
    "hookPerformance": "{{AGENT_HOME}}/social-marketing/hook-performance.json",
    "skillUpdates": "{{AGENT_HOME}}/social-marketing/skill-updates.json",
    "strategy": "{{AGENT_HOME}}/social-marketing/strategy.json",
    "competitorResearch": "{{AGENT_HOME}}/social-marketing/competitor-research.json"
  }
}
